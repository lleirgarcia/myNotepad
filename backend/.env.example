# OpenAI (get a key at https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-key-here

# Optional: server port (default 3000)
PORT=3000

# API key: frontend sends this (as VITE_BACKEND_API_KEY) to call /api/todos and /api/whiteboard.
# No login; backend persists data in Supabase under DEFAULT_USER_ID.
BACKEND_API_KEY=dfjbdafd-adsf-dsadsfasfd-asdfa

# Single "user" UUID for data when using API key (no Google login). Generate one at https://www.uuidgenerator.net/
DEFAULT_USER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Supabase: backend uses service role to read/write (no user credentials in frontend).
# Get service_role key from Supabase Dashboard → Settings → API (use "service_role" secret, not anon).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# —— Google OAuth (optional: if set, "Sign in with Google" is enabled) ——
# Create OAuth 2.0 Client ID at https://console.cloud.google.com/apis/credentials (Web application).
# Authorized redirect URIs: http://localhost:3000/api/auth/google/callback and https://YOUR-RAILWAY-URL/api/auth/google/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# JWT secret for signing login tokens (required if Google OAuth is enabled). Use a long random string.
JWT_SECRET=your-long-random-jwt-secret

# Frontend origin (for OAuth redirect_uri whitelist). Default: http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Optional: public backend URL (e.g. https://xxx.up.railway.app). Set in production so OAuth callback URL is correct.
# BACKEND_URL=https://your-app.up.railway.app

# —— Real API tests (npm run test:api) ——
# Run tests against a live backend. Default base URL: https://mynotepad-production.up.railway.app
# E2E_API_BASE_URL=https://mynotepad-production.up.railway.app
# E2E_API_KEY=<same value as BACKEND_API_KEY for that backend>
