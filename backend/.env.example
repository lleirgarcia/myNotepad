# OpenAI (get a key at https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-key-here

# Optional: server port (default 3000)
PORT=3000

# API key: frontend sends this (as VITE_BACKEND_API_KEY) to call /api/todos and /api/whiteboard.
# No login; backend persists data in Supabase under DEFAULT_USER_ID.
BACKEND_API_KEY=dfjbdafd-adsf-dsadsfasfd-asdfa

# Single "user" UUID for all data (personal use). Generate one at https://www.uuidgenerator.net/
DEFAULT_USER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Supabase: backend uses service role to read/write (no user credentials in frontend).
# Get service_role key from Supabase Dashboard → Settings → API (use "service_role" secret, not anon).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
